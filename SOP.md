# Primus Brief 页面 Markdown 语法 SOP

## 整体结构

```md
> 顶部左侧标签 | 顶部右侧小字

# Hero 大标题，**橙色高亮词**

Hero 描述文字...

- Label: Value
- Label: Value
- Label: Value
- Label: Value

== [主按钮文字](链接) [次按钮文字](链接) ==

## 章节标题

章节 lead 描述...

### 卡片标题 [徽章]
> 卡片灰色小字描述（可选）

卡片正文段落...

== [按钮文字](链接) ==

### 下一张卡片 [徽章]
...

!!!TIMELINE:标题 [徽章]!!!
- 日期 | 内容 | 类型标签 | 链接
- 日期 | 内容 | 类型标签 | 链接

!!!LINKS!!!
- [链接文字](链接) | 右侧标签
- [链接文字](链接) | 右侧标签

## 下一个章节
...
```

---

## 语法速查表

| 元素 | 语法 | 层级 | 说明 |
|---|---|---|---|
| Hero 顶部标签 | `> 左侧文字 \| 右侧小字` | 页面级 | 写在 `#` 上面 |
| Hero 大标题 | `# 标题 **高亮词**` | 页面级 | `**` 包的词变橙色 |
| Hero 描述 | 标题下第一段正文 | 页面级 | 自动识别 |
| Hero Meta 行 | `- Label: Value` 无序列表 | 页面级 | 4 列，放在描述下面 |
| Hero 按钮 | `== [文字](url) [文字](url) ==` | 页面级 | 第一主色第二描边 |
| 章节 | `## 章节名` | section 级 | 自动进左侧 TOC，自动大写 |
| 卡片 | `### 标题 [徽章]` | 卡片级 | `[文字]` 是左上角徽章 |
| 卡片描述 | 卡片内第一段 `> 引用文字` | 卡片级 | 灰色斜体小字 |
| 卡片按钮 | 卡片内 `== [文字](url) ==` | 卡片级 | 橙色胶囊按钮 |
| 卡片内链接行 | 卡片内 `- [文字](url) \| 标签` 列表 | 卡片级 | 左右布局行 |
| 时间线列表 | `!!!TIMELINE:标题 [徽章]!!!` + 列表 | section 平级 | 日期+内容+tag+链接 |
| 链接卡片网格 | `!!!LINKS!!!` + 列表 | section 平级 | 两列卡片网格，左右布局 |

---

## 规则说明

1. **所有 `!!!XXX!!!` 标记的组件**（TIMELINE / LINKS）都和卡片平级，属于 section 的独立元素，不会被收进卡片里
2. **卡片以 `###` 开头**，默认到下一个 `###` 或下一个 `##` 结束，不需要显式结束标记
3. **`== ... ==` 是按钮行**，写在卡片里是卡片按钮，写在 hero 里是 hero 按钮，写在 section 层级（所有卡片外）是 section CTA
4. **timeline 每行格式**：`日期 | 内容 | 类型标签 | 链接`，用 `|` 分隔，链接可为空
5. **link card 每行格式**：`[文字](url) | 右侧标签`，用 `|` 分隔
6. **所有组件顺序和 md 里写的顺序一致**，想怎么排就怎么排

---

## 文件

| 文件 | 作用 |
|---|---|
| `index.html` | 页面骨架 |
| `styles.css` | 所有样式 |
| `app.js` | 渲染逻辑（markdown → DOM） |
| `content.md` | **全部文案**，唯一需要改的文件 |
| `logo-orange.png` | 左侧 logo |
| `banner2.png` | hero 背景图 |

## 本地运行

```bash
cd /Users/fubiaoxia/works11/understandprimus
python3 -m http.server 8000
# 打开 http://localhost:8000
```
